import {
	makeNotification,
	hideNotification
} from "./actionCreator";

export const addNotification = (data, notificationType) => {
	return dispatch => {
		let text = data.text // todo here
	}
};

export const deleteNotifications = (id) => {
	return dispatch => {

	}
};

